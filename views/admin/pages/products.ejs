<%- include('../partials/header') %>

<div class="page-header">
  <h1 class="page-title">Products</h1>
  <a href="/admin/products/create" class="admin-btn admin-btn-primary">Add Product</a>
</div>

<div class="admin-card">
  <div class="admin-card-body">
    <div class="toolbar">
      <div class="toolbar-left">
        <form action="/admin/products" method="GET" class="admin-search">
          <i class="fas fa-search"></i>
          <input type="text" name="search" placeholder="Search products..." value="<%= search || '' %>">
        </form>
        <select name="category" form="filterForm" class="form-input" onchange="this.form.submit()">
          <option value="">All Categories</option>
          <% if (typeof categories !== 'undefined') { %>
            <% categories.forEach(function(cat) { %>
            <option value="<%= cat.id %>" <%= filterCategory == cat.id ? 'selected' : '' %>><%= cat.name %></option>
            <% }) %>
          <% } %>
        </select>
      </div>
      <div class="toolbar-right">
        <form id="bulkForm" action="/admin/products/bulk" method="POST">
          <select name="action" class="form-input" required>
            <option value="">Bulk Actions</option>
            <option value="delete">Delete</option>
            <option value="activate">Activate</option>
            <option value="deactivate">Deactivate</option>
            <option value="feature">Feature</option>
          </select>
          <button type="submit" class="admin-btn admin-btn-sm">Apply</button>
        </form>
      </div>
    </div>

    <form id="filterForm" action="/admin/products" method="GET">
      <input type="hidden" name="search" value="<%= search || '' %>">
      <input type="hidden" name="category" value="<%= filterCategory || '' %>">
    </form>
  </div>
</div>

<div class="admin-card">
  <div class="admin-card-body">
    <table class="admin-table">
      <thead>
        <tr>
          <th width="40"><input type="checkbox" id="selectAll"></th>
          <th width="50">Image</th>
          <th>Name</th>
          <th>SKU</th>
          <th>Category</th>
          <th>Price</th>
          <th>Stock</th>
          <th>Status</th>
          <th width="200">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% if (typeof products !== 'undefined' && products.length > 0) { %>
          <% products.forEach(function(product) { %>
          <tr>
            <td><input type="checkbox" name="selectedIds" form="bulkForm" value="<%= product.id %>" class="select-item"></td>
            <td>
              <% if (product.thumbnail) { %>
                <img src="<%= product.thumbnail %>" alt="<%= product.name %>" class="product-thumb">
              <% } else { %>
                <i class="fas fa-gem"></i>
              <% } %>
            </td>
            <td><a href="/admin/products/<%= product.id %>/edit"><%= product.name %></a></td>
            <td><%= product.sku || '-' %></td>
            <td><%= product.categoryName || '-' %></td>
            <td>$<%= (product.price || 0).toFixed(2) %></td>
            <td>
              <% if (product.stock <= 0) { %>
                <span class="admin-badge admin-badge-danger">Out of Stock</span>
              <% } else if (product.stock <= (product.lowStockThreshold || 5)) { %>
                <span class="admin-badge admin-badge-warning"><%= product.stock %></span>
              <% } else { %>
                <span><%= product.stock %></span>
              <% } %>
            </td>
            <td>
              <% var badgeClass = product.isActive ? 'admin-badge-success' : 'admin-badge-secondary' %>
              <span class="admin-badge <%= badgeClass %>"><%= product.isActive ? 'Active' : 'Inactive' %></span>
            </td>
            <td>
              <a href="/admin/products/<%= product.id %>/edit" class="admin-btn admin-btn-sm" title="Edit"><i class="fas fa-edit"></i></a>
              <form action="/admin/products/<%= product.id %>/duplicate" method="POST" style="display:inline">
                <button type="submit" class="admin-btn admin-btn-sm" title="Duplicate"><i class="fas fa-copy"></i></button>
              </form>
              <form action="/admin/products/<%= product.id %>/toggle-status" method="POST" style="display:inline">
                <button type="submit" class="admin-btn admin-btn-sm" title="Toggle Status"><i class="fas fa-power-off"></i></button>
              </form>
              <form action="/admin/products/<%= product.id %>?_method=DELETE" method="POST" style="display:inline" data-confirm="Are you sure you want to delete this product?">
                <button type="submit" class="admin-btn admin-btn-danger admin-btn-sm" title="Delete"><i class="fas fa-trash"></i></button>
              </form>
            </td>
          </tr>
          <% }) %>
        <% } else { %>
          <tr><td colspan="9" class="text-center">No products found</td></tr>
        <% } %>
      </tbody>
    </table>
  </div>
</div>

<% if (typeof pagination !== 'undefined' && pagination.pages > 1) { %>
<div class="admin-pagination">
  <span>Showing <%= pagination.from %> to <%= pagination.to %> of <%= pagination.total %></span>
  <div class="pagination-links">
    <% if (pagination.current > 1) { %>
      <a href="?page=<%= pagination.current - 1 %>&search=<%= search || '' %>&category=<%= filterCategory || '' %>" class="admin-btn admin-btn-sm">&laquo; Prev</a>
    <% } %>
    <% for (var i = 1; i <= pagination.pages; i++) { %>
      <a href="?page=<%= i %>&search=<%= search || '' %>&category=<%= filterCategory || '' %>" class="admin-btn admin-btn-sm <%= pagination.current == i ? 'admin-btn-primary' : '' %>"><%= i %></a>
    <% } %>
    <% if (pagination.current < pagination.pages) { %>
      <a href="?page=<%= pagination.current + 1 %>&search=<%= search || '' %>&category=<%= filterCategory || '' %>" class="admin-btn admin-btn-sm">Next &raquo;</a>
    <% } %>
  </div>
</div>
<% } %>

<script>
  document.getElementById('selectAll').addEventListener('change', function() {
    document.querySelectorAll('.select-item').forEach(function(cb) {
      cb.checked = this.checked;
    }, this);
  });

  document.querySelectorAll('[data-confirm]').forEach(function(form) {
    form.addEventListener('submit', function(e) {
      if (!confirm(this.getAttribute('data-confirm'))) {
        e.preventDefault();
      }
    });
  });
</script>

<%- include('../partials/footer') %>